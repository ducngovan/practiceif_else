function getcheck() {
    var reply;
    reply=document.getElementById('check').value;
    if (reply=="ECMAScript"){
        alert("right");
    }else{
        alert("Didn’t know? ECMAScript!");
    }
}